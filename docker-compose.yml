version: '3'

services:
  pandoc-converter:
    image: pandoc/minimal:latest-alpine
    entrypoint: ""
    command: sh -c 'ls *.ipynb | xargs -I {fn} pandoc /data/{fn} -o ./views/{fn}.html'
    volumes:
      - .:/data